pluginManagement {
    repositories {
        mavenCentral()
        maven { url 'https://repo.spring.io/release' }
        gradlePluginPortal()
    }
}

rootProject.name = 'ledger-sync'
include 'components:common'
include 'components:consumer-common'
include 'components:scheduler'

include 'application'
include 'scheduler-app'
include 'streamer-app'
